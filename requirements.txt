# ==========================
# ResilientVisionAutonomous
# Unified requirements for:
#   - attack_engine / dataset_generator / dataset_loader (attack pipeline)
#   - models (YOLO detection/evaluation pipeline, Ultralytics)
#   - src/ (CNN attack-detection pipeline: simulator, dataset, training)
# Compatible with Python 3.10+, Windows and Linux (incl. NVIDIA H100).
# ==========================

# Deep learning (CNN detector + YOLO wrapper)
torch>=2.0.0
torchvision>=0.15.0
ultralytics>=8.3.0

# Numerical computing
numpy>=1.24.0

# Image processing
Pillow>=10.0.0
opencv-python>=4.8.0

# Configuration
PyYAML>=6.0

# Metrics (training/evaluation of the CNN detector)
scikit-learn>=1.3.0

# Visualization and analysis (scripts + exploration.ipynb)
matplotlib>=3.8.0
pandas>=2.0.0

# Progress bars
tqdm>=4.65.0

# Testing
pytest>=8.0.0

# ---- Optional, not required for the core pipelines ----
# Kaggle KITTI download helper (run.py --mode download):
# kaggle>=1.6.0
# nuScenes frame extraction (run.py --mode extract_nuscenes):
# nuscenes-devkit>=1.1.10
# safety_mechanism ROS 2 demos (nuscenes_publisher.py, stub_cnn_publisher.py):
#   requires a ROS 2 installation providing rclpy (not pip-installable)
# safety_mechanism dashboard demo (demo_dashboard2.py):
# streamlit>=1.30.0
